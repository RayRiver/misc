module network
{
	void network_start @ start();
	void network_stop @ stop();
	void network_connect @ connect(const char *hostname, unsigned short port);
	void network_disconnect @ disconnect();
	void network_send @ send(unsigned int id, const LuaValueList &args);
	void network_registerHandler @ registerHandler(LUA_FUNCTION handler);
	void network_unregisterHandler @ unregisterHandler();
}
